import React from 'react'
import { components } from 'react-select'

export const CustomSelectSingleValue = ({ data, ...props }) => (
  <components.SingleValue {...props}>a</components.SingleValue>
)
